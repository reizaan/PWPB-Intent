<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.belajarintent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BelajarIntent">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntentActivity"/>
        <activity android:name=".LatihanGridLayout"/>
        <activity android:name=".SoalGridLayout"/>
        <activity android:name=".UI1Activity"/>
        <activity android:name=".UI2Activity"/>
        <activity android:name=".ListViewActivity"/>
        <activity android:name=".InputUserActivity"/>
    </application>

</manifest>